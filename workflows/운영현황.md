# 경기북부 기상·재난 상황판 운영 현황

> **새 세션 첫 메시지 템플릿:**
> ```
> C:\Users\USER\Desktop\AI업무처리\기상,재난상황판 프로젝트야.
> CLAUDE.md와 workflows/운영현황.md 읽고 이어서 작업해줘.
> 오늘 할 일: ___
> ```

---

## 시스템 개요

- **사이트 URL (운영)**: https://gyeonggi-dashboard.visanu81.workers.dev/  ← Cloudflare Pages (2026-05-20 이전)
- **사이트 URL (구)**: ~~https://visanu81.github.io/gyeonggi-dashboard/~~ (정부망에서 차단됨, 사용 중단)
- **GitHub 저장소**: visanu81/gyeonggi-dashboard
- **운영자**: 소방위 한경승 (동두천소방서)
- **자동화**: GitHub Actions cron이 매시간 데이터 수집 → push → Cloudflare 자동 배포 (PR #1 merge 후)
- **이전 이유**: 정부 사내망 방화벽이 GitHub Pages CDN IP대역(`185.199.x.x`) 차단. Cloudflare(`172.67.x.x`, `104.21.x.x`)는 통과 → 한국 CDN 노드라 응답 1초 내

## 지역 확장 — 한 코드로 두 지역 (2026-08-02)

경기남부 상황판을 추가했다. **코드는 하나**고 '지역마다 다른 값'만 갈아끼운다.
지금까지처럼 화면·수집기를 통째로 복사했다면 앞으로 기능을 고칠 때마다 두 번 고쳐야 하고,
한쪽만 고치는 실수가 반드시 난다.

| | 경기북부 (운영) | 경기남부 (신규) |
|---|---|---|
| 주소 | gyeonggi-dashboard.visanu81.workers.dev | **weather.visanu81.workers.dev** |
| Cloudflare Worker | `gyeonggi-dashboard` | `weather` |
| 관할 | 10개 시군 · 11개 관서(고양/일산 분리) | 21개 시군 |
| 데이터 파일 | `data.js` | `data-south.js` |
| 지도 | `map-geo.js` | `map-geo-south.js` |
| 화면 설정 | `region.js` | `region-south.js` |
| 텔레그램 알림 | 보냄 | 안 보냄(북부 구독자에게 남부 특보가 가면 안 됨) |

**지역별 값이 사는 곳**
- 뒷단(수집): `tools/region_profiles.py` — 시군·격자·관측소·특보구역·하천·산불권역
- 앞단(화면): `region.js` / `region-south.js` — 관서 순서·배치도·행정명·중점 관서

**돌리는 법**
```
python tools/update_data.py                            # 경기북부 → data.js
python tools/update_data.py --profile south --no-push  # 경기남부 → data-south.js
python tools/update_data.py --push-only                # 커밋·푸시·배포 (마지막에 한 번)
```
서버 `update.sh`는 두 지역을 **동시에** 돌린 뒤 한 번만 푸시한다(순서대로 돌리면 5분을 넘김).
중복 실행 방지 lock도 걸어 뒀다.

**만드는 도구 (다시 뽑을 때)**
```
python tools/build_region_profile.py --verify                    # 북부 운영값 재현 검증
python tools/build_region_profile.py --build south --check-rivers # 남부 값 생성 + 하천 현실성 검사
python tools/build_map_geo.py --verify                            # 지도 생성기 검증
python tools/build_map_geo.py --profile south --merge --out map-geo-south.js
python tools/verify_profiles.py                                   # 북부 값이 안 바뀌었는지 대조
```
생성기는 모두 **경기북부 운영값을 그대로 재현하는지 먼저 검증**한 뒤 남부를 만든다.
재현이 안 되면 남부 값도 믿을 수 없다는 뜻이다.

**API 호출량** (5분 주기, 실측)
- 북부 51회/회차, 남부 71회/회차 → 하루 약 35,000건 (공공데이터포털분은 약 15,000건)
- 단기예보는 하루 8번만 발표되는데 5분마다 부르고 있었다 → 발표시각 기준 캐시로 97% 감축.
  덕분에 **지역이 둘로 늘었는데도 예전 북부 단독 때보다 호출이 적다.**

**주의**
- 남부에서 걸러낸 것: 김포 사우교 수위관측소(조위 영향으로 평상시에도 관심수위 초과 → 매일 주의 표시).
  `tools/build_region_profile.py`의 `RIVER_EXCLUDE` 참조.
- 관내에 수위관측소가 없는 5개 시(과천·시흥·군포·의왕·하남)는 인근 관측소를 '인근'으로 표기해 붙였다.

### 상황실 상단바 '임진강 감시' 카드 (2026-08-04)

필승교·군남댐은 관서 순환과 무관하게 늘 봐야 하는 지점이라(필승교=북측 황강댐 무단방류
조기감지, 군남댐 방류량=하류 임진강 수위) 상단바에 **고정 카드**로 올렸다.
전에는 연천 차례가 돌아올 때만 보였다.

상단바 칸 배분 (무대 안쪽 폭 3728px)
```
전: 1120 | 1fr(1400) | 460 | 640                    ← 관서명·날씨가 내용보다 넓었음
후:  700 | 1fr(924)  | 860 | 460 | 640   (북부)
     700 | 1fr(1820) |     | 460 | 640   (남부 — 카드 없음)
```
칸이 좁아진 만큼 글자도 줄였다(관서명 150→118px, 기온 170→140px). 안 줄이면 `nowrap`이라 잘린다.

**어느 지점을 띄울지는 `region.js`의 `topRivers`가 정한다** — 한강홍수통제소 관측소 코드로
지정(표기명은 바뀔 수 있어도 코드는 안 바뀜). 비우면 카드가 사라지고 4칸으로 돌아간다.
남부는 비워 뒀다 — 넣고 싶으면 `region-south.js`에 코드만 적으면 된다.

구현은 `tools/wall-build/build_wall.py`의 `(k1)~(k5)` 패치. `design_clean.html`(사장님 디자인
원본)은 건드리지 않는다 — 디자인을 다시 받으면 수정이 날아가기 때문.

### 저장소 비대화 · 히스토리 압축 (2026-08-05 개선)

**문제** — 5분마다 데이터를 커밋하니 저장소가 하루 수백MB씩 커진다. 그래서 서버가
'커밋 300개 넘으면 히스토리를 1커밋으로 눌러 강제 푸시'하고 있었다(거의 하루 한 번).
이 압축은 공통 조상을 없애 **개발 PC의 클론에서 rebase가 불가능해진다** — 압축 커밋이
내 작업을 되돌리는 것처럼 보이는 충돌이 실제로 났다.

**실측한 1회차 커밋 비용 (약 1.6MB → 하루 460MB)**

| 파일 | 크기 | 비고 |
|---|---|---|
| `images/kma_satellite.png` | **907KB** | 전체의 57% |
| `data-south.js` | 358KB | |
| `data.js` | 269KB | |
| 레이더·강수예측·특보발효도 | 55KB | 합계 |

**고친 것 3가지**

1. **위성영상을 30분 주기로** (`tools/update_data.py`)
   천리안 위성은 **6시간 지연 공개** 자료다. 5분마다 받아 커밋할 이유가 없었다.
   파일이 30분 안 지났으면 건너뛴다 → **하루 커밋량 460MB → 240MB (절반)**.
   건너뛴 회차는 이전 이미지·메타를 그대로 쓰므로 화면은 그대로다.
   로그에 `갱신 주기 아님`으로 찍히니 장애로 오해하지 말 것.

2. **압축 판단을 '커밋 수' → '실제 .git 용량'으로** (`update.sh`)
   커밋 수는 용량의 대리지표일 뿐이라 디스크가 멀쩡해도 압축이 돌았다.
   이제 `.git`이 **1200MB**를 넘을 때만 압축한다(커밋 2000개는 백스톱).
   위 1번과 합쳐 **압축 주기가 하루 1회 → 약 5일에 1회**로 줄어든다.

3-1. **개발 PC 용량 회수** (`tools/dev_clean.sh`, 2026-08-11 추가)
   압축이 일어나도 개발 PC에 남은 옛 브랜치·작업폴더가 압축 전 히스토리를 붙들고 있어
   `.git`이 계속 커진다(실측 5.0GB — 대부분이 7월 이전 `staging` 브랜치 10,470커밋).
   ```
   bash tools/dev_clean.sh          # 무엇을 지울지 보여주기만
   bash tools/dev_clean.sh --run    # 실제 정리
   ```
   지우기 전에 브랜치마다 "origin/main 에 없는 파일"이 있는지 확인하고, 있으면 건너뛴다.
   바탕화면의 작업폴더(`기상,재난상황판-2`)는 **지우지 않고** 원격 최신으로 맞춘다.

3. **개발 클론 동기화 도구** (`tools/dev_sync.sh`)
   압축이 일어나도 rebase로 씨름하지 말 것. 코드의 정답은 언제나 `origin/main`이다.
   ```
   bash tools/dev_sync.sh          # origin/main 에 맞추기
   bash tools/dev_sync.sh --gc     # 맞춘 뒤 옛 객체 정리(용량 회수)
   ```
   커밋 안 한 변경이 있으면 멈추고, 되돌리기용 `dev-backup` 브랜치를 남긴다.

### 배포할 때만 커밋 (2026-08-11 개선)

위 항목의 '앞으로 더 줄이려면'을 실행한 것. 5분마다 커밋했지만 실제 배포는
20~40분에 한 번이라, 나머지 커밋은 **아무 화면에도 안 나가는 데이터**였다.

**바꾼 것**

1. `tools/update_data.py` — 배포 판단을 실행에서 분리(`deploy_decision`).
   `--push-only`는 이제 **배포하기로 결정한 회차에만** 커밋·푸시한다.
   배포 안 하는 회차의 변경은 파일로 남아 있다가 다음 배포 때 한 커밋으로 같이 올라간다.
   → 화면에 나가는 데이터는 그대로(배포 직전 회차가 항상 최신), 커밋 수만 1/4~1/8.

2. `update.sh` — 회차 시작의 `git reset --hard origin/main`을 **조건부**로.
   무조건 되돌리면 아직 커밋 안 한 데이터를 5분마다 지워버린다.
   이제 `origin/main`이 우리보다 앞서 있을 때(코드 배포·히스토리 압축)만 되돌린다.
   ```
   if ! git merge-base --is-ancestor origin/main HEAD; then git reset --hard origin/main; fi
   ```
   히스토리 압축 블록에도 `git add -A`를 넣었다 — 안 그러면 압축이 옛 데이터로 되돌린다.

3. 겸사겸사 **배포 판단이 두 지역을 같이 본다**. 배포 한 번이 북부·남부 사이트를
   같이 올리는데 판단은 북부 data.js만 보고 있었다. 남부에만 호우특보가 떠도
   최대 40분 늦던 것이 이제 즉시 나간다.

**주의** — 배포 사이에는 서버 작업트리가 '더러운' 상태로 보이는 게 정상이다
(`git status`에 data.js·이미지가 변경됨으로 뜸). 장애가 아니다.

### '⚠ N분째 갱신 없음' 배지 기준 (2026-08-11)

지도 화면 우하단의 빨간 배지는 **화면 데이터가 오래됐을 때** 뜬다(서버·회선 장애 감지용).
기준이 15분이었는데 **화면은 배포될 때만 바뀌고 그 주기가 평소 40분·급변특보중 20분**이라,
평상시에도 배지가 떠서 오경보가 됐다(2026-08-11 사장님 발견).

→ 기준을 **50분**으로 올렸다. 평소 최악 = 배포간격 40분 + 수집 3분 + 배포반영 2분 ≈ 45분,
여기에 경계 깜빡임을 막을 5분 여유. `지도.html`의 `dataStale` 참조.

⚠ **배포 주기(`deploy_decision`)를 당기면 이 값도 같이 내려야 한다.** 둘은 짝이다.
안 그러면 배지가 장애를 늦게 알린다.

참고 — 진짜 급한 특보·하천 위험은 화면이 아니라 **1분 주기 텔레그램 알리미**
(`tools/check_warnings.py`)가 담당한다. 화면 주기와는 별개 경로다.

## ★★ 상황실에 '가짜 호우 재난문자'가 떴던 일 (2026-08-15)

**사장님 신고** — "연천에 호우주의보가 떴다는데 확인해줘."

**사실** — 기상청엔 경기북부 호우특보가 **0건**이었다(그날 전국 호우주의보는 제주도산지 1곳).
그런데 상황실 동두천 화면에 이런 재난문자가 떠 있었다:

> 동두천시 · 호우 · 13:36 — "**호우주의보 발효.** 하천변·지하공간·지하주차장 접근을 금지하고…"
> 경기도 · 호우 · 12:40 — "시간당 **1.1mm의 강한 비**가 내리고 있습니다…[경기도청]"

**원인** — `design_clean.html`(사장님이 Claude Design에서 받은 시안)은 시연용으로
**비가 오면 재난문자를 지어낸다.** 실데이터를 아예 안 본다.
```js
if (s.wet){ msgs = [ {text:'호우주의보 발효. …'}, {text:'시간당 '+s.rain1h+'mm의 강한 비…'} ]; }
else      { msgs = ((EXT().msgs || MSGS)[s.name] || …) }   // ← 실데이터는 여기만
```
문자 없는 관서에 샘플이 뜨는 건 이미 막아뒀지만(`msgs`에 전 관서 키를 빈 배열로 채움),
**이 분기는 `EXT().msgs`를 거치지 않아서 그 방어를 그냥 통과했다.**
"1.1mm의 강한 비"라는 말이 안 되는 문장이 가짜라는 결정적 단서였다.

**조치** — `build_wall.py`에 패치 (j7) 추가. 강수 여부와 무관하게 **항상 실제 재난문자만**
쓰고, 없으면 안 띄운다. 겸해서 문자 종류별 색을 넣었다(호우=파랑, 강풍=주황).

**교훈 — 디자인 시안의 '지어낸 값'은 전부 봉인 대상이다.**
시안은 그림이 예뻐야 해서 데이터를 만들어낸다. 운영에선 그게 거짓말이 된다.
새 디자인을 받으면 **반드시 아래를 훑을 것**:

| 확인할 것 | 방법 |
|---|---|
| 값을 지어내는 분기 | `design_clean.html`에서 `EXT()`를 안 거치는 대입, `Math.random`, `rnd(`, `r1~r3` |
| 샘플로 폴백하는 자리 | `\|\| MSGS`, `\|\| RIVERS`, `\|\| RISKS`, `\|\| REGIME` |
| 폴백이 실제로 막혔는지 | 브라우저에서 `WALL_DATA`의 `stations·rivers·msgs·risks·warns`에 **전 관서 키가 다 있는지** |

2026-08-15 점검 결과 — 네 묶음 모두 11개 관서 키 전부 존재(폴백 차단됨), `stations`도 11개.
재난문자가 마지막 구멍이었다.

## ★ 배포 절차 — 테스트2 먼저 (2026-08-07 확정)

**반드시 테스트2에 먼저 올리고, 사장님이 눈으로 확인한 뒤 운영에 반영한다.**

| 사이트 | 주소 | 올리는 브랜치 |
|---|---|---|
| **테스트2** (먼저) | gyeonggi-dashboard-2.visanu81.workers.dev | `staging` |
| 운영 · 경기북부 | gyeonggi-dashboard.visanu81.workers.dev | `main` → `deploy-trigger` |
| 운영 · 경기남부 | weather.visanu81.workers.dev | `main` → `deploy-trigger` |

```bash
# ① 테스트2에 올리기 — 사장님 확인용
git push -f origin <작업브랜치>:staging

# ② 사장님 확인 후에만 운영으로
git push origin <작업브랜치>:main
git push -f origin <작업브랜치>:deploy-trigger
```

**주의 — 2026-08-07에 실제로 생긴 일**
`staging` 브랜치가 원격에서 사라져 있었고(누가 지웠는지 불명), 그 사실을 모른 채
`main`/`deploy-trigger`로만 밀어 **여러 변경이 확인 없이 곧장 운영에 반영됐다.**
테스트2는 그동안 옛 버전에 멈춰 있었다(`region.js` 404). 지금은 복구했다.
→ 배포 전 `git ls-remote --heads origin staging` 으로 브랜치 존재를 먼저 확인할 것.

**테스트2 특성**
- 화면 파일만 테스트 버전이고 **데이터는 운영 것을 실시간으로 그대로 본다**.
  그래서 데이터가 아니라 '화면이 어떻게 보이는지'를 확인하는 용도다.
- 운영과 완전히 분리된 Worker라 여기서 뭘 해도 운영에 영향이 없다.

**브라우저 캐시 주의**
화면 파일이 바뀌어도 브라우저가 옛 파일을 계속 쓴다. 확인할 땐 **Ctrl+F5**.
(실제로 이것 때문에 '반영이 안 됐다'고 오판할 뻔했다)

## 데이터 흐름 (2026-05-20 이후)

```
[GitHub Actions cron — 매시간]      [사장님 PC update_data.py — 5분마다 (옵션)]
   ↓                                   ↓
[7개 API 수집]                      [7개 API 수집 + 한강홍수통제소]
   ↓                                   ↓
[data.js + 상황판.html 자동 생성]   [git push (정부망에선 차단되어 누적될 수 있음)]
   ↓
[main에 push]
   ↓
[Cloudflare Pages가 push 감지 → 자동 배포 (~1분, 빌드 명령 없는 정적 사이트)]
   ↓
[https://gyeonggi-dashboard.visanu81.workers.dev/]
   ↓
[동료들은 URL로 항상 최신 데이터 확인 — 정부망에서도 접속 가능]
```

⚠️ **중요:**
- 한강홍수통제소 API는 한국 IP 필요. GitHub Actions 환경에서 차단됨 → 사장님 PC가 보완해서 수집·push (옵션).
- 사장님 PC가 꺼져있어도 GitHub Actions cron(PR #1)이 매시간 자동 갱신 → 시스템 항상 살아있음.

## 완성된 기능 (11개 API 연동)

| API | 출처 | 데이터 |
|---|---|---|
| 단기예보 | 기상청 공공API (data.go.kr) | 시군별 24시간 시간별 예보 |
| 초단기실황 | 기상청 공공API | 실측 기온·풍속·강수 |
| 기상특보 (공공) | 기상청 공공API (WthrWrnInfoService getPwnStatus) | 발효(t6) + 예비(t7) 파싱 — 호우·태풍 누락 있음 |
| **기상특보 (본청 신 API)** | **기상청 API 허브 wrn_met_data.php** | **본청 신 발표문 — 호우·태풍 예비특보까지 완전 수집, STN별·LVL별 상태머신** |
| 통보문·기상정보 | 기상청 공공API | 특보 전문 + 기상정보 이력 |
| 미세먼지 | 환경공단 에어코리아 | 10개 시군 PM10·PM2.5 |
| 산불 위험도 | 국립산림과학원 forestPointV2 | 권역별 4단계 |
| 산불발생통계 | 산림청 forestStusService | 최근 1년 발생 내역 |
| 하천 수위 | 한강홍수통제소 (waterlevel/dam) | 7개 하천(신천 동두천·연천, 임진강 필승교·임진교, 한탄강 용담교, 왕숙천, 중랑천) + 군남댐 (24h history) |
| 홍수예보 발령 | 한강홍수통제소 fldfct | 주의보·경보 발령 정보 |
| 재난문자 | 행안부 safetydata.go.kr | 경기도 전체 (북부 우선) |

⚙️ **예비특보 처리 메모 (2026-05-20 추가):**
- `getPwnStatus` 응답의 `t7` 필드에 텍스트로 들어있음. 별도 endpoint 아님.
- 형식: `(1) 강풍 예비특보\no 시간 : 지역\no ...` → `parse_prelim_warnings()`로 그룹·항목 파싱
- 경기북부 10개 시군 키워드 매칭으로 `north_match` 플래그 부여
- 광역 모드: 카운트만 (`발효 N · 예비 M`), 본문 한 줄 안내
- 내관서 중점 모드: 그룹별 상세 표시, 북부 매칭 항목은 노란 점 강조
- 영향예보(Impact-based)는 별도 endpoint 추측 모두 "API not found" → 공공 API 미제공으로 보류

## UI 구성

### 헤더
- 진한 네이비 + 노란 사이렌 로고 박스
- 토글: 광역 / 내관서 중점
- 시군 변경 버튼 (내관서 모드)
- 📡 기상영상 버튼 (기상청 레이더/위성/낙뢰 iframe 모달)
- 🚨 알람 테스트 / 🔊 음소거 / 🌙 테마 토글
- 시계 (큰 노란 글씨)

### KPI 바 6칸
종합상태 / 발효특보 / 재난문자 / 동두천 기온 / 신천 수위 / 군남댐 저수율

### 광역 모드 그리드 (3x3)
```
시군별 기상 (col 1, 3행 전체)  │  발효 특보  │  재난문자
                              │  산불 위험  │  하천 수위
                              │  시간대별 예보 (col 2-3 row 3)
```

### 내관서 중점 모드 (3x3 + 4행 가로 전체)
- 시군 선택 모달 (10개 시군)
- 1행: 현재 기상 / 인근 수위 (여러 하천) / 미세먼지
- 2행: 12시간 예보 / 산불 / 발효 특보
- 3행: 재난문자 / 광역 특보
- 4행: 24시간 시간대별 상세 표 (가로 전체)

### 모달 (6종)
1. 시군 카드 클릭 → 시군 상세 (초단기실황, 누적강수)
2. 하천 클릭 → 24시간 차트 + CCTV(또는 댐 자료 링크)
3. 미세먼지 패널 → 시군별 비교 차트
4. 산불 패널 → 권역별 + 발생 내역 + 4단계 설명
5. 재난문자 항목 → 메시지 전문
6. 발효 특보 패널 → 통보문 + 기상정보 이력

### 알림 시스템
- 펄스: 경보 배너 한 곳만 (나머지는 정적 색 변경)
- 사운드: Web Audio API 자체 비프 (severe=3연속, warning=2연속, 일반=1)
- 음소거 토글 + 🚨 테스트 버튼
- 종합상태 KPI 칸은 글로우 없이 색만

### 디자인 톤
- **라이트 모드 (기본)**: 베이지 본문 + 흰 카드 + 진한 네이비 헤더 + 노란 강조 — 공공기관 PoC 톤
- **다크 모드 (옵션)**: NORAD 톤 (관제실 야간 운영용)

### 시각화
- 시간대별 예보: SVG 라인 차트 (Bloomberg 스타일)
- 하천 수위: 24시간 미니 sparkline (각 항목 안)
- 시군 카드: 좌측 4px 컬러 바 (위험도)

## 파일 구조

```
기상,재난상황판/
├── index.html              # 메인 (1700+ 줄, CSS+JS 인라인)
├── 상황판.html              # 공유용 스냅샷 (data 인라인)
├── data.js                 # 자동 생성 (압축, ~60KB)
├── .env                    # API 키 3개 (절대 push X)
├── .gitignore              # .env, .tmp/* 보호
├── 갱신.bat                # 수동 갱신
├── 상황판열기.bat          # 평소 사용
├── 자동갱신_백그라운드.vbs  # 작업 스케줄러용
├── 서버시작.bat            # 사내망 서버 (옵션)
├── tools/
│   ├── update_data.py      # 데이터 수집 + git push (~700줄)
│   └── server.py           # 사내망 미니 서버 (옵션)
├── workflows/
│   └── 운영현황.md          # 이 문서
├── .github/workflows/
│   └── update.yml          # Pages 배포만 (push 트리거)
└── .tmp/
    ├── rain_history.json   # 시간별 강수 누적
    └── update.log
```

## API 키 (.env)

```
DATA_GO_KR_KEY=e0c33...d696    # data.go.kr 통합 (단기예보·미세먼지·산불·통보문)
HRFCO_KEY=AE8D9593-...         # 한강홍수통제소
SAFETY_DATA_KEY=ZGA279940...   # safetydata.go.kr (행안부 재난문자)
```

⚠️ GitHub Secrets에도 동일 등록되어 있음 (사용 안 함, 백업용).

## 자동화 설정

### 작업 스케줄러
- 작업명: `GyeonggiBukbu_Dashboard_AutoUpdate`
- 간격: 5분
- 실행: `자동갱신_백그라운드.vbs` (조용히 update_data.py 실행)
- 로그: `.tmp/update.log`

### GitHub Actions
- `.github/workflows/update.yml`
- 트리거: push to main + workflow_dispatch
- 작업: GitHub Pages 배포만 (~30초)
- 데이터 수집 X (사장님 PC가 담당)

## 알려진 이슈 / 보류 사항

- 산불발생통계 API는 올해(2026) 0건 — 작년 데이터까지만 누적 (실시간 X). 정상.
- 한강홍수통제소 API는 GitHub Actions 환경에서 차단됨. 사장님 PC만 호출 가능. (하이브리드 방식으로 회피)
- 산불상황 관제 iframe 버튼은 사장님 요청으로 제거됨 (실시간 아님).

## 보안 메모

- PAT는 `.git/config`에 저장됨 (사장님 PC에만)
- 폴더 통째로 외부 공유 시 PAT 노출 위험 — 카톡·메일 X
- API 키 노출 시 즉시 폐기 후 재발급

## 마지막 작업 시점 상태 (2026-05-20)

**호스팅 이전 완료:**
- ~~GitHub Pages~~ → **Cloudflare Pages** (`gyeonggi-dashboard.visanu81.workers.dev`)
- 이유: 정부 사내망에서 GitHub Pages CDN(`185.199.x.x`) 차단됨
- Cloudflare는 한국 노드(서울/부산) 사용, 응답 1초 내, 무료 플랜 충분
- 빌드 실패 원인이었던 `.claude/worktrees/` submodule 참조 제거 (commit `f05595d`)
- `.gitignore`에 `.claude/` 추가

**UI/UX 개선 (이번 세션):**
- 광역 모드 그리드 재배치: 시군별(col1 row1-3) | 특보·산불·재난문자 세로 스택(col2) | 하천 row1-4 span(col3) | 시간대별 예보 row4(col1-2)
- 하천 카드 압축 (sparkline 28→20px, 항목 패딩·gap 축소)
- 디자인 톤 세련화: shadow 3단(sm/md/lg), radius 변수, 패널 호버 lift, 헤더 그라데이션, KPI 좌측 그라데이션 바
- 시간대별 예보 차트: 직선 → 베지어 곡선, 그라데이션 영역, 점 마커 2겹, `preserveAspectRatio xMidYMid meet`(stretch 제거), max-width 700px
- 다크 모드 KPI를 라이트 모드 카드 구조로 통일 (균형감)
- CCTV iframe 비율 처리: viewport 강제 + scale·translate, 데스크탑·태블릿·모바일 4단계 분리
- 모바일 헤더 4줄 구조 (광역/내관서 토글 100% 폭 → 시군변경+아이콘 버튼 → 실시간 운영 → 시계)
- 테마: 항상 라이트로 시작 (sessionStorage, 새 탭 열면 라이트로 리셋)

**KPI 바 시군 동적 갱신 (이번 세션):**
- 광역 모드 = 동두천 고정 (기본 관서)
- 내관서 중점 모드 = `CURRENT_FOCUS_REGION` 기반 동적
- 카드 3개 다 시군 따라 라벨·데이터 변경:
  - **기온**: `{시군} 기온`
  - **하천 수위**: `REGION_RIVER_MATCH`로 인근 하천 매핑 → 첫 매칭 강명 (예: 의정부 → "중랑천 수위", 포천 → "한탄강 수위")
  - **댐/보조 수위**: 인근 댐 있으면 저수율 (예: 동두천·연천 = 군남댐), 없으면 인근 두 번째 하천 수위
- `setView()`·`selectFocusRegion()`에서 `renderKpiBar(DATA)` 호출로 즉시 갱신
- 라벨 id 부여 (`kpiTempLabel`·`kpiRiverLabel`·`kpiDamLabel`)

**카드 정렬 — 모드별 분리 (이번 세션):**
- **광역 모드**: 기존 grid 배치 유지 (사장님 선호) — `panel-region` row1-3 span + `panel-warning/fire/msg` col 2 stacked + `panel-river` row1-3 span + `panel-forecast` row 4 가로 전체
- **내관서 중점 모드**: Pinterest CSS Multi-column (`column-count: 3`, `break-inside: avoid`) — 카드 콘텐츠 높이에 맞춰 자동 배치
- 반응형:
  - 데스크탑 ≥1200px: 광역 3열 grid / 내관서 3컬럼
  - 태블릿 768~1199px: 광역 2열 grid + 행 재배치 / 내관서 2컬럼
  - 모바일 <768px: 광역 1열 / 내관서 1컬럼

**Cloudflare 자동 배포 — GitHub Actions로 영구 해결 (이번 세션):**
- 원인: Cloudflare Worker가 "Disconnected from Git" 상태 → push에도 자동 배포 안 됨 (마지막 wrangler 수동 배포 후 5시간+ 정지)
- 해결: `.github/workflows/cloudflare-deploy.yml` + `wrangler.toml` 추가 → push마다 GitHub Actions가 wrangler-action으로 배포
- wrangler.toml: `[assets].directory = "."` (정적 자산 = 프로젝트 루트 전체)
- 워크플로 동시성: `concurrency.group: cloudflare-deploy`로 큐 처리 (5분 자동 push가 겹쳐도 안전)

⚠️ **사장님 1회 설정 필요 (Cloudflare API Token + GitHub Secrets 등록)**:
1. Cloudflare 대시보드 → 우측 상단 프로필 → **"My Profile"** → **"API Tokens"** 탭
2. **"Create Token"** → 템플릿 **"Edit Cloudflare Workers"** 선택 → 권한 자동 설정
3. **Account Resources**: `Include - All accounts` 또는 본인 account 선택
4. **"Continue to summary"** → **"Create Token"** → 발급된 토큰 복사 (한 번만 보임!)
5. **Account ID** 확인: 대시보드 메인 화면 우측 사이드바 또는 URL `dash.cloudflare.com/{accountId}/...`
6. **GitHub Secrets 등록**: github.com/visanu81/gyeonggi-dashboard → Settings → Secrets and variables → Actions → New repository secret
   - `CLOUDFLARE_API_TOKEN` = 위 4번에서 복사한 토큰
   - `CLOUDFLARE_ACCOUNT_ID` = 위 5번에서 확인한 Account ID

이 설정이 끝나면 사장님 PC 켜져있든 꺼져있든 GitHub Actions가 5분마다 데이터 갱신 + Cloudflare에 자동 배포. 사이트 24시간 자동 운영.

**레이더 + 위성 영상 추가 (이번 세션):**
- 레이더: apihub `typ04/url/rdr_cmp_file.php?data=img&cmp=cmb` — 한반도 강수 분포 컬러맵 (5분 주기, ~110KB PNG)
  - 시각 정렬: 15분 전 + 5분 배수 (`minute // 5 * 5`)
  - 폴백: 5/10/15/30분 더 과거로 재시도 (file not exist 대응)
- 위성: apihub `typ05/api/GK2A/LE1B/IR105/KO/image` — 천리안 2A 적외(10.5μm) 한반도 (10분 주기, ~800KB PNG)
  - 자료 publish 약 6시간 지연 → 6/7/8/9/10/12/18/24시간 전 폴백 (10분 배수 정렬)
- 둘 다 PNG magic byte 검증 + 사장님 PC만 호출 (키 노출 X)
- `download_kma_images()`에 블록 3) 레이더 + 4) 위성 추가
- 내관서 중점 row 4 `focus-panel-radar` 자동 활성화 — placeholder 사라지고 실시간 한반도 강수 컬러맵 표시
- 모달 탭 `📡 레이더 (KMA)` + `🛰️ 위성 (적외)` 추가

**내관서 중점 모드 — 이미지 카드 통합 (이번 세션 추가):**
- 광역과 100% 중복이던 두 카드 제거 + 기상 이미지 카드로 교체:
  - `focus-panel-broad` (광역 발효 특보) → **`focus-panel-warnimg`** : 🌐 전국 특보 발효도 (`images/kma_warn.png`)
  - `focus-panel-hourly` (24시간 표) → **`focus-panel-radar`** : 📡 레이더 합성영상 (`images/kma_radar.png`, 단계 B)
- 새 JS 함수 `updateFocusImageCards()` — img.src cache-bust + 갱신시각 배지 + ok=false 시 placeholder
- 카드 클릭 → 기존 `openWeatherImgModal(initialTab)` 호출, 해당 탭으로 직접 진입
- `WEATHER_IMG_SOURCES`에 `'radar-img'` 추가 + 모달 탭에 `📡 레이더 (KMA)` 추가
- 새 CSS: `.focus-img-body`, `.focus-img-empty`, `.focus-radar-body` (이미지 카드 전용)
- 모바일 반응형: 카드 풀폭 + max-height 240px

⚠️ **레이더 단계 B 활성화 절차** (사장님이 진행):
1. https://apihub.kma.go.kr 로그인 → "API 목록" → 카테고리 "레이더" 클릭
2. "레이더 합성" 검색 → endpoint 후보(`nph-rdr_cmp_inf_img` 등) 신청
3. 승인 후 정확한 endpoint URL + 필수 파라미터 회신
4. `tools/update_data.py` `download_kma_images()`에 레이더 블록 3개 추가 (기존 wrn/qpf 패턴 복사)
5. 갱신.bat 실행 → `images/kma_radar.png` 생성 → 자동 표시

**기상청 API 허브 통합 (이번 세션 추가):**
- apihub.kma.go.kr 인증키(`KMA_APIHUB_KEY`) `.env`에 추가
- **사장님 승인 완료 API (7개)**:
  1. `wrn_now_data_new.php` — **현재 발효+예비특보 종합** (메인 사용 중 ✓)
  2. `wrn_reg.php` — 특보구역 코드 → 시군명 매핑 (정적 매핑 추출에 사용 ✓)
  3. `wrn_met_data.php` — 특보 이력 (보조, 현재 미사용)
  4. **`nph-wrn7`** — 전국 특보 발효 현황 이미지 (기상영상 모달에 통합 ✓)
  5. **`nph-qpf_ana_img`** — 초단기 강수예측 이미지 (기상영상 모달에 통합 ✓)
  6. `getUltraSrtNcst` — 동네예보 (이미 공공 API에서 사용 중, 중복)
  7. `nph-dfs_vsrt_grd` — 격자자료 (고급 분석용, 향후 검토)

- `fetch_apihub_warnings()` 메인 endpoint: **`wrn_now_data_new.php`**
  - 응답: EUC-KR 텍스트 CSV, REG_KO/WRN/LVL/CMD 한글 그대로
  - 해제·만료 자동 제외 (현재 살아있는 발표만)
  - `data.apihub_warnings`로 전체 저장 → 클라이언트에서 예비특보(LVL='예비')는 `prelim_warnings` 구조로 변환, 경기북부 발효는 `warnings`에 dedup 보강

- **REG_ID 정적 매핑** (`NORTH_GG_REG_MAP` + `GG_SOUTH_REG_MAP`):
  - 경기북부 10개: L1011100(동두천)·L1011200(연천)·L1011300(포천)·L1011400(가평)·L1011500(고양)·L1011600(양주)·L1011700(의정부)·L1011800(파주)·L1012200(구리)·L1012300(남양주)
  - 경기 남부 21개: L1010200~L1013500
  - 광역: L1010000(경기도)

- 예시 결과 (2026-05-20 오후 호우 예비특보 발효): 🚨 **연천·파주** + 🟡 안산·시흥·김포·평택·화성

- **이미지 API 통합 흐름** (키 노출 X):
  - `download_kma_images()`: 사장님 PC가 매시간 apihub에서 PNG 받아 `images/kma_warn.png`·`images/kma_qpf.png`에 저장
  - git push로 같이 올라감 → 사이트는 그 이미지 파일을 `<img src="images/kma_xxx.png">`로 표시
  - 클라이언트엔 키 노출 안 됨 (사장님 PC만 apihub 호출)
  - 기상영상 모달에 **🚨 특보 발효도** + **💧 강수 예측** 새 탭 2개 추가, 갱신 시각 표시

⚠️ **GitHub Actions cron 동작을 위해 GitHub Secrets에도 `KMA_APIHUB_KEY` 등록 필요**: repo Settings → Secrets and variables → Actions → New secret

**기상청 공공 API 한계 보완 (이번 세션 추가):**
- 공공 API `getPwnStatus` 응답에 호우·태풍 예비특보가 누락되는 경우 발견 (강풍·풍랑 위주만 들어옴)
- 신 endpoint 후보(`getPreWthrWrnList`, `getPwnSituationList` 등 7개) 모두 "API not found"
- 날씨누리(weather.go.kr)는 JS 렌더링 SPA라 정적 스크래핑 불가
- **차선책**: 헤더에 `🌐 날씨누리` 빠른 진입 버튼 + 발효 특보 패널 하단에 `📡 날씨누리에서 더 보기 ↗` 링크 추가
- `NORTH_GG_KEYWORDS`에 `'경기도'`·`'수도권'` 키워드 추가 — 광역 표기로 발표되는 예비특보도 매칭되도록

**홍수예보 발령 통합 (이번 세션 추가):**
- 한강홍수통제소 `fldfct` endpoint: `http://api.hrfco.go.kr/{KEY}/fldfct/list.json`
- 평상시 응답 `code=990` (자료없음) — 발령 시에만 `links` 배열에 항목 채워짐
- 응답 필드: `ANCDT`(발표일시), `KIND`(주의보/경보), `RVRNM`(강명), `OBSNM`(지점), `FCTDT`(도달예상시각), `STTCURHGT`(현재수위), `WRNARANM`(주의지역)
- 경기북부 매칭 키워드: 10개 시군 + 임진강·한탄강·신천·왕숙천·중랑천
- 광역 모드 `panel-river`: 발령 시 카드 본문 상단에 빨강/주황 알람 띠 (전국 카운트) → 클릭하면 모달
- 내관서 중점 `focus-panel-river`: **북부 매칭 발령만** 강조 알람 띠 → 클릭하면 모달
- 모달: 북부 매칭 항목 위로 정렬, 노란 배경 강조
- 영향예보(Impact-based)는 별도 endpoint 미공개로 보류

**예비특보 통합 (이번 세션 추가):**
- `update_data.py`에 `parse_prelim_warnings()` 추가 — t7 텍스트 → 구조화된 그룹·항목
- `data.warnings` (발효 배열)·`data.prelim_warnings` (예비 그룹 배열) 분리
- 광역 모드 `panel-warning`: 헤더 "기상특보 (발효 · 예비)", 본문에 발효 + 예비 한 줄 안내
- 내관서 중점 `focus-panel-warning`: "[시군명] 특보 (발효+예비)" 동적 제목, 시군 매칭 발효·예비 모두 상세
- 내관서 중점 `focus-panel-broad`: 광역 발효 + 예비 그룹별 상세
- 새 CSS: `.prelim-divider`, `.prelim-group`, `.prelim-item.north-match` (북부 매칭 노란 점)

**테스트 모드 기능 추가:**
- 헤더 `TEST` 버튼 토글 (자동 시작 X, 클릭 시에만 시작)
- 활성 시: 빨강 펄스 + "TEST ON" + 남은 시간 tooltip
- 가짜 호우경보(severe) + 강풍주의보(warning) 발효 + 가짜 재난문자 1건
- 화면 가운데 "테스트" 워터마크 (220px, -22도, 빨강 반투명)
- 3시간 후 자동 종료
- 콘솔 헬퍼: `testNow()`, `testStop()`, `testStatus()`

**경계 반복 알람:**
- severe 발효 시 7초 간격 "뚜뚜뚜" 반복
- 음소거(🔊) 클릭 시 정지, 해제 시 자동 재개
- severe 사라지면 자동 정지

## 다음 단계 후보

1. **outstanding 브랜치 정리**: `claude/connect-existing-code-b2UjP` 원격 브랜치 (이미 main에 merge됨) — Cloudflare 경고 메시지 제거용
2. 시군 카드 / 차트 추가 미세 조정
3. **별도 프로젝트**: 동두천시 재난안전지도 (이미 별도 폴더로 운영 중)
   - 폴더: `C:\Users\USER\Desktop\AI업무처리\동두천시 재난안전지도\`
   - 로컬 + Firebase + GitHub Pages 하이브리드
4. self-hosted runner 또는 사장님 PC update_data.py 안정화 (한강홍수통제소 보완)

## 새 세션에서 이어 작업하는 법

```
이 프로젝트 폴더: C:\Users\USER\Desktop\AI업무처리\기상,재난상황판
GitHub: visanu81/gyeonggi-dashboard
운영 URL: https://visanu81.github.io/gyeonggi-dashboard/

CLAUDE.md와 workflows/운영현황.md 먼저 읽어줘.

오늘 할 일:
- ___ (디자인 추가 손보기 / 새 기능 / 다른 프로젝트 등)
```

새 Claude는 이 문서 + CLAUDE.md + 파일 구조 보고 5분 안에 컨텍스트 파악 가능.
